/// <reference types="vite/client" />

// Injected by Vite at build time (see vite.config.ts): the ISO timestamp of the
// build, so the app can show when this exact bundle was produced.
declare const __BUILD_AT__: string;
